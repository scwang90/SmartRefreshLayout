apply plugin: 'com.android.library'

description = 'the header TwoLevelHeader of SmartRefreshLayout'

apply from: "${rootDir.path}/gradle/base.gradle"

dependencies {
    api project(':refresh-header-classics')
    compileOnly project(':refresh-layout-kernel')
}
